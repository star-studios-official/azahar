interface:
  display_name: "macOS Telemetry Probe"
  short_description: "Add privacy-safe macOS runtime telemetry"
  default_prompt: "Use $macos-telemetry-probe to add focused Logger instrumentation for macOS windows, sidebar, menu, and menu bar actions, then verify the emitted events with Console or log stream after running the app."
